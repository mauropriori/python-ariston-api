[build-system]
build-backend = "flit_core.buildapi"
requires = ["flit_core >=3.2,<4"]

[project]
name = "ariston"
readme = "README.md"
authors = [
    { name = "Tamás Füstös", email = "fustom@gmail.com" }
]
dependencies = [
    "aiohttp",
    "requests"
]
requires-python = ">=3.10"
license = { file = "LICENSE" }
version = "0.20.0"
dynamic = ['description']

[project.urls]
Repository = "https://github.com/mauropriori/python-ariston-api.git"
Issues = "https://github.com/mauropriori/python-ariston-api/issues"
